# Supabase — client-side (inlined at build time)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server-side (read at runtime, required for Vercel)
SUPABASE_URL=
SUPABASE_ANON_KEY=
